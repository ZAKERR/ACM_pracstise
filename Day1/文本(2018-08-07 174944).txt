#include<cstdio>#include<iostream>#include<cstring>#include<algorithm>using namespace std;int pre[5]={0,3,2,3,2};int abs(int x){	if(x>0)return x;	return -x;}int work(int n,int m){	}int main(){	int t;	scanf("%d",&t);	while(t--)	{		int n,m;		scanf("%d%d",&n,&m);		n=abs(n);m=abs(m);		if(n>m)swap(n,m);//n小m大		work(n,m); 		long long ans=0;		/*while(n>0)		{			n--;			m-=2;			ans++;		}*/		ans+=n;m-=(n*2);		m=abs(m);//n=0,m>=0		if(m<=4)		{			ans+=pre[m];			printf("%lld\n",ans);		}		else		{			int tmp=m/4;m%=4;			ans+=2*tmp;ans+=pre[m];			printf("%lld\n",ans);		}			}	return 0;}